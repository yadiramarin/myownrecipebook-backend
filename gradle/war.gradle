apply plugin: "war"

bootWar {
    mainClass = "com.myownrecipebook.MyOwnRecipeBookApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
